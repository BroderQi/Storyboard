<Window x:Class="Storyboard.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="clr-namespace:Storyboard.Converters"
    xmlns:pages="clr-namespace:Storyboard.Views.Pages"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    mc:Ignorable="d"
    Title="Storyboard Editor" Height="900" Width="1800"
    WindowStartupLocation="CenterScreen" WindowState="Maximized"
    Background="#F0F2F5">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

            <!-- 顶部标题栏 -->
            <Border Grid.Row="0" Padding="24,16" Background="White" BorderBrush="{StaticResource Gray200}" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="📹" FontSize="24" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="Storyboard Editor" FontSize="18" FontWeight="Bold" Foreground="{StaticResource Slate800}"/>
                                <TextBlock Text="v2.6.1" FontSize="12" Foreground="{StaticResource Gray400}" Margin="8,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="{Binding Shots.Count, Mode=OneWay, StringFormat=共 {0} 个分镜 · 拖拽镜头号调整顺序}" 
                                       Foreground="{StaticResource Gray500}" FontSize="11" Margin="0,2,0,0"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="24,0,0,0">
                        <RadioButton x:Name="NavStoryboard" Content="分镜管理" IsChecked="True" GroupName="TopNavGroup" Style="{StaticResource TopNavRadioButton}"/>
                        <RadioButton x:Name="NavUsageDoc" Content="使用文档" GroupName="TopNavGroup" Style="{StaticResource TopNavRadioButton}" Margin="12,0,0,0"/>
                        <RadioButton x:Name="NavApiKey" Content="密钥配置" GroupName="TopNavGroup" Style="{StaticResource TopNavRadioButton}" Margin="12,0,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                        <Rectangle Width="1" Height="16" Fill="{StaticResource Gray300}" Margin="0,0,16,0"/>
                        <Button Content="⚙️" FontSize="18" Background="Transparent" BorderThickness="0" 
                                Width="32" Height="32" Padding="6" Margin="0,0,16,0" Cursor="Hand"/>
                       
                    </StackPanel>
                </Grid>
            </Border>

        <!-- 内容区域 -->
        <Grid Grid.Row="1">
            <pages:StoryboardPage x:Name="StoryboardPage" Visibility="{Binding IsChecked, ElementName=NavStoryboard, Converter={StaticResource BoolToVisibility}}"/>
            <pages:UsageDocPage x:Name="UsageDocPage" Visibility="{Binding IsChecked, ElementName=NavUsageDoc, Converter={StaticResource BoolToVisibility}}"/>
            <pages:ApiKeyPage x:Name="ApiKeyPage" Visibility="{Binding IsChecked, ElementName=NavApiKey, Converter={StaticResource BoolToVisibility}}"/>
        </Grid>

        <!-- 加载遮罩 -->
        <Grid Grid.RowSpan="2" Background="#CC000000" Visibility="{Binding IsAnalyzing, Converter={StaticResource BoolToVisibility}}">
            <Border Background="#1E1E1E" BorderBrush="#0078D4" BorderThickness="2" 
                    CornerRadius="8" Padding="40,32"
                    HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="🔄" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                    <TextBlock Text="AI 正在分析视频..." FontSize="20" Foreground="#E0E0E0" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                    <TextBlock Text="请稍候，这可能需要几分钟时间..." FontSize="12" Foreground="#808080" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
