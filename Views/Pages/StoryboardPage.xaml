<UserControl x:Class="Storyboard.Views.Pages.StoryboardPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:behaviors="clr-namespace:Storyboard.Behaviors"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <Border Grid.Row="0" Background="White" CornerRadius="12" Padding="16" Margin="20,20,20,20"
                BorderBrush="{StaticResource Gray100}" BorderThickness="1">
            <StackPanel Orientation="Horizontal">
                <Button Command="{Binding UploadVideoCommand}" Margin="0,0,12,0" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="{StaticResource Blue600}"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="16,8"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="📤" FontSize="16" Margin="0,0,8,0"/>
                                                <TextBlock Text="上传视频"/>
                                            </StackPanel>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource Blue700}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Margin="0,0,12,0" Cursor="Hand" Command="{Binding AddShotCommand}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="Foreground" Value="{StaticResource Slate700}"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="BorderBrush" Value="{StaticResource Gray200}"/>
                            <Setter Property="Padding" Value="16,8"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="➕" FontSize="16" Margin="0,0,8,0"/>
                                                <TextBlock Text="新增分镜"/>
                                            </StackPanel>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource Gray50}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Margin="0,0,12,0" Cursor="Hand" Command="{Binding BatchGenerateImagesCommand}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="Foreground" Value="{StaticResource Slate700}"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="BorderBrush" Value="{StaticResource Gray200}"/>
                            <Setter Property="Padding" Value="16,8"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="✨" FontSize="16" Foreground="{StaticResource Orange500}" Margin="0,0,8,0"/>
                                                <TextBlock Text="批量生成图片"/>
                                            </StackPanel>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource Gray50}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Cursor="Hand" Command="{Binding BatchGenerateVideosCommand}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="Foreground" Value="{StaticResource Slate700}"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="BorderBrush" Value="{StaticResource Gray200}"/>
                            <Setter Property="Padding" Value="16,8"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="🎬" FontSize="16" Foreground="{StaticResource Purple600}" Margin="0,0,8,0"/>
                                                <TextBlock Text="一键合成视频"/>
                                            </StackPanel>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource Gray50}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Border>

        <!-- 主数据表格 -->
        <Border Grid.Row="1" Background="White" BorderBrush="{StaticResource Gray100}" BorderThickness="1" Margin="20,0,20,16" CornerRadius="12">
            <DataGrid Style="{x:Null}"
                          ItemsSource="{Binding Shots}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserReorderColumns="False"
                          CanUserResizeRows="False"
                          SelectionMode="Single"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          RowHeight="84"
                          FontSize="14"
                          ColumnHeaderHeight="44"
                          RowHeaderWidth="0"
                          SnapsToDevicePixels="True"
                          FrozenColumnCount="1"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          Background="White"
                          Foreground="{StaticResource Slate800}"
                          BorderThickness="0"
                          HorizontalGridLinesBrush="{StaticResource Gray100}"
                          VerticalGridLinesBrush="Transparent"
                          ColumnHeaderStyle="{StaticResource LightDataGridColumnHeaderStyle}"
                          CellStyle="{StaticResource LightDataGridCellStyle}"
                          RowStyle="{StaticResource LightDataGridRowStyle}"
                          behaviors:DataGridShotReorderBehavior.IsEnabled="True">

                    <DataGrid.Columns>
                        <!-- #列（固定列） -->
                        <DataGridTextColumn Header="#" Binding="{Binding ShotNumber}" Width="60" IsReadOnly="True">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="FontSize" Value="14"/>
                                    <Setter Property="FontFamily" Value="Consolas,Courier New"/>
                                    <Setter Property="Foreground" Value="#9CA3AF"/>
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- 时长（可编辑） -->
                        <DataGridTemplateColumn Header="时长" Width="90">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Duration, StringFormat={}{0:0.##}}" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding Duration, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>

                        <!-- 首帧提示词（可编辑，多行） -->
                        <DataGridTemplateColumn Header="首帧提示词" Width="280">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding FirstFramePrompt}" TextWrapping="Wrap" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding FirstFramePrompt, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>

                        <!-- 尾帧提示词（可编辑，多行） -->
                        <DataGridTemplateColumn Header="尾帧提示词" Width="280">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding LastFramePrompt}" TextWrapping="Wrap" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding LastFramePrompt, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>

                        <!-- 镜头类型（可编辑） -->
                        <DataGridTemplateColumn Header="镜头类型" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding ShotType}" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding ShotType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>

                        <!-- 核心画面（可编辑） -->
                        <DataGridTemplateColumn Header="核心画面" Width="220">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding CoreContent}" TextWrapping="Wrap" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding CoreContent, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>

                        <!-- 动作指令（可编辑） -->
                        <DataGridTemplateColumn Header="动作指令" Width="200">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding ActionCommand}" TextWrapping="Wrap" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding ActionCommand, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>

                        <!-- 场景设定（可编辑） -->
                        <DataGridTemplateColumn Header="场景设定" Width="240">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding SceneSettings}" TextWrapping="Wrap" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding SceneSettings, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>

                        <!-- 选用模型（可编辑） -->
                        <DataGridTemplateColumn Header="选用模型" Width="160">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding SelectedModel}" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <ComboBox SelectedValue="{Binding SelectedModel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                              SelectedValuePath="Content"
                                              VerticalContentAlignment="Center">
                                        <ComboBoxItem Content="RunwayGen3"/>
                                        <ComboBoxItem Content="Pika"/>
                                        <ComboBoxItem Content="Stable Diffusion"/>
                                    </ComboBox>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>

                        <!-- 首帧图：无图显示生成，有图显示缩略图 + 查看/打开位置 -->
                        <DataGridTemplateColumn Header="首帧图" Width="240" IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.Style>
                                            <Style TargetType="Grid">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </Style>
                                        </Grid.Style>

                                        <!-- 无图：生成按钮 -->
                                        <Button Content="🎨 生成首帧" Padding="12,6" Cursor="Hand"
                                                Command="{Binding DataContext.GenerateFirstFrameAsyncCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Background" Value="White"/>
                                                    <Setter Property="Foreground" Value="{StaticResource Slate700}"/>
                                                    <Setter Property="BorderBrush" Value="{StaticResource Gray200}"/>
                                                    <Setter Property="BorderThickness" Value="1"/>
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}"
                                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                                        CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding FirstFrameImagePath, Converter={StaticResource InverseBoolToVisibility}}" Value="Visible">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsFirstFrameGenerating}" Value="True">
                                                            <Setter Property="Content" Value="生成中..."/>
                                                            <Setter Property="IsEnabled" Value="False"/>
                                                        </DataTrigger>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="{StaticResource Gray50}"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>

                                        <!-- 有图：缩略图 + 操作 -->
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <StackPanel.Style>
                                                <Style TargetType="StackPanel">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding FirstFrameImagePath, Converter={StaticResource InverseBoolToVisibility}}" Value="Visible">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </StackPanel.Style>

                                            <Border Width="56" Height="56" CornerRadius="8" BorderBrush="{StaticResource Gray100}" BorderThickness="1" Margin="0,0,12,0" ClipToBounds="True">
                                                <Image Source="{Binding FirstFrameImagePath}" Stretch="UniformToFill"/>
                                            </Border>

                                            <StackPanel VerticalAlignment="Center">
                                                <Button Content="查看大图" Padding="10,4" Margin="0,0,0,6" Cursor="Hand"
                                                        Command="{Binding DataContext.ViewFirstFrameImageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"/>
                                                <Button Content="打开位置" Padding="10,4" Cursor="Hand"
                                                        Command="{Binding DataContext.OpenFirstFrameFolderCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- 尾帧图：无图显示生成，有图显示缩略图 + 查看/打开位置 -->
                        <DataGridTemplateColumn Header="尾帧图" Width="240" IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <!-- 无图：生成按钮 -->
                                        <Button Content="🎨 生成尾帧" Padding="12,6" Cursor="Hand"
                                                Command="{Binding DataContext.GenerateLastFrameAsyncCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Background" Value="White"/>
                                                    <Setter Property="Foreground" Value="{StaticResource Slate700}"/>
                                                    <Setter Property="BorderBrush" Value="{StaticResource Gray200}"/>
                                                    <Setter Property="BorderThickness" Value="1"/>
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}"
                                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                                        CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding LastFrameImagePath, Converter={StaticResource InverseBoolToVisibility}}" Value="Visible">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsLastFrameGenerating}" Value="True">
                                                            <Setter Property="Content" Value="生成中..."/>
                                                            <Setter Property="IsEnabled" Value="False"/>
                                                        </DataTrigger>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="{StaticResource Gray50}"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>

                                        <!-- 有图：缩略图 + 操作 -->
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <StackPanel.Style>
                                                <Style TargetType="StackPanel">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding LastFrameImagePath, Converter={StaticResource InverseBoolToVisibility}}" Value="Visible">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </StackPanel.Style>

                                            <Border Width="56" Height="56" CornerRadius="8" BorderBrush="{StaticResource Gray100}" BorderThickness="1" Margin="0,0,12,0" ClipToBounds="True">
                                                <Image Source="{Binding LastFrameImagePath}" Stretch="UniformToFill"/>
                                            </Border>

                                            <StackPanel VerticalAlignment="Center">
                                                <Button Content="查看大图" Padding="10,4" Margin="0,0,0,6" Cursor="Hand"
                                                        Command="{Binding DataContext.ViewLastFrameImageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"/>
                                                <Button Content="打开位置" Padding="10,4" Cursor="Hand"
                                                        Command="{Binding DataContext.OpenLastFrameFolderCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- 视频：无视频显示生成，有视频显示封面 + 观看/打开位置 -->
                        <DataGridTemplateColumn Header="视频" Width="260" IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <!-- 无视频：生成按钮 -->
                                        <Button Content="🎬 生成视频" Padding="12,6" Cursor="Hand"
                                                Command="{Binding DataContext.GenerateVideoAsyncCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Background" Value="White"/>
                                                    <Setter Property="Foreground" Value="{StaticResource Slate700}"/>
                                                    <Setter Property="BorderBrush" Value="{StaticResource Gray200}"/>
                                                    <Setter Property="BorderThickness" Value="1"/>
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}"
                                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                                        CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding GeneratedVideoPath, Converter={StaticResource InverseBoolToVisibility}}" Value="Visible">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsVideoGenerating}" Value="True">
                                                            <Setter Property="Content" Value="生成中..."/>
                                                            <Setter Property="IsEnabled" Value="False"/>
                                                        </DataTrigger>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="{StaticResource Gray50}"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>

                                        <!-- 有视频：封面 + 操作 -->
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <StackPanel.Style>
                                                <Style TargetType="StackPanel">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding GeneratedVideoPath, Converter={StaticResource InverseBoolToVisibility}}" Value="Visible">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </StackPanel.Style>

                                            <Border Width="72" Height="56" CornerRadius="8" BorderBrush="{StaticResource Gray100}" BorderThickness="1" Margin="0,0,12,0" ClipToBounds="True">
                                                <Grid>
                                                    <Image Source="{Binding FirstFrameImagePath}" Stretch="UniformToFill"/>
                                                    <Border Background="#66000000"/>
                                                    <TextBlock Text="▶" FontSize="18" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Grid>
                                            </Border>

                                            <StackPanel VerticalAlignment="Center">
                                                <Button Content="观看视频" Padding="10,4" Margin="0,0,0,6" Cursor="Hand"
                                                        Command="{Binding DataContext.PlayVideoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"/>
                                                <Button Content="打开位置" Padding="10,4" Cursor="Hand"
                                                        Command="{Binding DataContext.OpenVideoFolderCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                        CommandParameter="{Binding}"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- 任务队列/历史 -->
        <Border Grid.Row="2" Background="White" BorderBrush="{StaticResource Gray100}" BorderThickness="1" Margin="20,0,20,16" CornerRadius="12" Padding="16">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                    <TextBlock Text="任务队列" FontWeight="SemiBold" Foreground="{StaticResource Slate800}" FontSize="14"/>
                    <TextBlock Text="（可取消：队列中/执行中）" Foreground="{StaticResource Slate500}" FontSize="12" Margin="8,2,0,0"/>
                </StackPanel>

                <DataGrid ItemsSource="{Binding JobHistory}" AutoGenerateColumns="False" CanUserAddRows="False"
                          HeadersVisibility="Column" GridLinesVisibility="Horizontal" RowHeight="34"
                          Background="White" Foreground="{StaticResource Slate800}" BorderThickness="0"
                          MaxHeight="220">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="时间" Binding="{Binding CreatedAt, StringFormat=HH:mm:ss}" Width="100" IsReadOnly="True"/>
                        <DataGridTextColumn Header="类型" Binding="{Binding TypeText}" Width="120" IsReadOnly="True"/>
                        <DataGridTextColumn Header="目标" Binding="{Binding TargetText}" Width="80" IsReadOnly="True"/>
                        <DataGridTextColumn Header="状态" Binding="{Binding StatusText}" Width="100" IsReadOnly="True"/>
                        <DataGridTextColumn Header="进度" Binding="{Binding Progress, StringFormat={}{0:P0}}" Width="90" IsReadOnly="True"/>
                        <DataGridTextColumn Header="错误" Binding="{Binding Error}" Width="*" IsReadOnly="True"/>
                        <DataGridTemplateColumn Header="操作" Width="90">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="取消" Padding="10,4" Command="{Binding DataContext.CancelJobCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}" IsEnabled="{Binding CanCancel}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>

        <!-- 底部统计栏 -->
        <Border Grid.Row="3" Background="White" BorderBrush="{StaticResource Gray100}" BorderThickness="0,1,0,0" Padding="24,12" Margin="20,0,20,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Foreground="{StaticResource Slate600}" FontSize="12" Margin="0,0,24,0">
                        <Run Text="总分镜 "/>
                        <Run Text="{Binding Shots.Count, Mode=OneWay}" FontWeight="Bold" Foreground="{StaticResource Slate800}"/>
                    </TextBlock>
                    <TextBlock Foreground="{StaticResource Slate600}" FontSize="12" Margin="0,0,24,0">
                        <Run Text="已生成 "/>
                        <Run Text="{Binding CompletedShotsCount}" FontWeight="Bold" Foreground="#16A34A"/>
                    </TextBlock>
                    <TextBlock Foreground="{StaticResource Slate600}" FontSize="12">
                        <Run Text="渲染中 "/>
                        <Run Text="{Binding ShotsRenderingCount}" FontWeight="Bold" Foreground="#F97316"/>
                    </TextBlock>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Foreground="{StaticResource Slate600}" FontSize="12">
                        <Run Text="🖼 已生成图像 "/>
                        <Run Text="{Binding GeneratedImagesCount}" FontWeight="Bold" Foreground="#16A34A"/>
                    </TextBlock>
                    <TextBlock Foreground="{StaticResource Slate600}" FontSize="12" Margin="24,0,0,0">
                        <Run Text="🎬 已生成视频 "/>
                        <Run Text="{Binding GeneratedVideosCount}" FontWeight="Bold" Foreground="#0078D4"/>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
