<UserControl x:Class="Storyboard.Views.Pages.ApiKeyPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <ScrollViewer Padding="40" Background="Transparent">
        <StackPanel MaxWidth="860">
            <TextBlock Text="🔑 AI 服务配置" FontSize="28" FontWeight="Bold" Foreground="{StaticResource Slate800}" Margin="0,0,0,18"/>
            <TextBlock TextWrapping="Wrap" Foreground="{StaticResource Gray500}" FontSize="12" Margin="0,0,0,24">
                配置将写入应用目录下的 appsettings.json，并立即刷新（无需重启即可“验证配置”）。
            </TextBlock>

            <!-- Default Provider -->
            <Border Background="White" BorderBrush="{StaticResource Gray200}" BorderThickness="1" CornerRadius="12" Padding="20" Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="默认 Provider" Foreground="{StaticResource Slate800}" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,16,0"/>
                    <ComboBox Grid.Column="1" ItemsSource="{Binding AvailableProviderTypes}" SelectedItem="{Binding DefaultProvider}" Height="34"/>
                </Grid>
            </Border>

            <TabControl>
                <!-- Qwen -->
                <TabItem Header="通义千问 (Qwen)">
                    <StackPanel Margin="16">
                        <CheckBox Content="启用" IsChecked="{Binding QwenEnabled}" Foreground="{StaticResource Slate700}" Margin="0,0,0,12"/>

                        <TextBlock Text="API Key" Foreground="{StaticResource Slate700}"/>
                        <TextBox Text="{Binding QwenApiKey}" Height="34" Background="White" Foreground="{StaticResource Slate800}" BorderBrush="{StaticResource Gray200}" Padding="10,6"/>

                        <TextBlock Text="默认模型" Foreground="{StaticResource Slate700}" Margin="0,12,0,0"/>
                        <TextBox Text="{Binding QwenDefaultModel}" Height="34" Background="White" Foreground="{StaticResource Slate800}" BorderBrush="{StaticResource Gray200}" Padding="10,6"/>

                        <TextBlock Text="Endpoint" Foreground="{StaticResource Slate700}" Margin="0,12,0,0"/>
                        <TextBox Text="{Binding QwenEndpoint}" Height="34" Background="White" Foreground="{StaticResource Slate800}" BorderBrush="{StaticResource Gray200}" Padding="10,6"/>

                        <TextBlock Text="超时（秒）" Foreground="{StaticResource Slate700}" Margin="0,12,0,0"/>
                        <TextBox Text="{Binding QwenTimeoutSeconds}" Height="34" Background="White" Foreground="{StaticResource Slate800}" BorderBrush="{StaticResource Gray200}" Padding="10,6"/>
                    </StackPanel>
                </TabItem>

                <!-- Wenxin -->
                <TabItem Header="文心一言 (Wenxin)">
                    <StackPanel Margin="16">
                        <CheckBox Content="启用" IsChecked="{Binding WenxinEnabled}" Foreground="{StaticResource Slate700}" Margin="0,0,0,12"/>

                        <TextBlock Text="API Key" Foreground="{StaticResource Slate700}"/>
                        <TextBox Text="{Binding WenxinApiKey}" Height="34" Background="White" Foreground="{StaticResource Slate800}" BorderBrush="{StaticResource Gray200}" Padding="10,6"/>

                        <TextBlock Text="API Secret" Foreground="{StaticResource Slate700}" Margin="0,12,0,0"/>
                        <TextBox Text="{Binding WenxinApiSecret}" Height="34" Background="White" Foreground="{StaticResource Slate800}" BorderBrush="{StaticResource Gray200}" Padding="10,6"/>

                        <TextBlock Text="默认模型" Foreground="{StaticResource Slate700}" Margin="0,12,0,0"/>
                        <TextBox Text="{Binding WenxinDefaultModel}" Height="34" Background="White" Foreground="{StaticResource Slate800}" BorderBrush="{StaticResource Gray200}" Padding="10,6"/>

                        <TextBlock Text="Endpoint" Foreground="{StaticResource Slate700}" Margin="0,12,0,0"/>
                        <TextBox Text="{Binding WenxinEndpoint}" Height="34" Background="White" Foreground="{StaticResource Slate800}" BorderBrush="{StaticResource Gray200}" Padding="10,6"/>

                        <TextBlock Text="超时（秒）" Foreground="{StaticResource Slate700}" Margin="0,12,0,0"/>
                        <TextBox Text="{Binding WenxinTimeoutSeconds}" Height="34" Background="White" Foreground="{StaticResource Slate800}" BorderBrush="{StaticResource Gray200}" Padding="10,6"/>
                    </StackPanel>
                </TabItem>

                <!-- Zhipu -->
                <TabItem Header="智谱 (Zhipu)">
                    <StackPanel Margin="16">
                        <CheckBox Content="启用" IsChecked="{Binding ZhipuEnabled}" Foreground="{StaticResource Slate700}" Margin="0,0,0,12"/>

                        <TextBlock Text="API Key" Foreground="{StaticResource Slate700}"/>
                        <TextBox Text="{Binding ZhipuApiKey}" Height="34" Background="White" Foreground="{StaticResource Slate800}" BorderBrush="{StaticResource Gray200}" Padding="10,6"/>

                        <TextBlock Text="默认模型" Foreground="{StaticResource Slate700}" Margin="0,12,0,0"/>
                        <TextBox Text="{Binding ZhipuDefaultModel}" Height="34" Background="White" Foreground="{StaticResource Slate800}" BorderBrush="{StaticResource Gray200}" Padding="10,6"/>

                        <TextBlock Text="Endpoint" Foreground="{StaticResource Slate700}" Margin="0,12,0,0"/>
                        <TextBox Text="{Binding ZhipuEndpoint}" Height="34" Background="White" Foreground="{StaticResource Slate800}" BorderBrush="{StaticResource Gray200}" Padding="10,6"/>

                        <TextBlock Text="超时（秒）" Foreground="{StaticResource Slate700}" Margin="0,12,0,0"/>
                        <TextBox Text="{Binding ZhipuTimeoutSeconds}" Height="34" Background="White" Foreground="{StaticResource Slate800}" BorderBrush="{StaticResource Gray200}" Padding="10,6"/>
                    </StackPanel>
                </TabItem>

                <!-- Volcengine -->
                <TabItem Header="火山引擎 (Volcengine)">
                    <StackPanel Margin="16">
                        <CheckBox Content="启用" IsChecked="{Binding VolcengineEnabled}" Foreground="{StaticResource Slate700}" Margin="0,0,0,12"/>

                        <TextBlock Text="API Key" Foreground="{StaticResource Slate700}"/>
                        <TextBox Text="{Binding VolcengineApiKey}" Height="34" Background="White" Foreground="{StaticResource Slate800}" BorderBrush="{StaticResource Gray200}" Padding="10,6"/>

                        <TextBlock Text="EndpointId" Foreground="{StaticResource Slate700}" Margin="0,12,0,0"/>
                        <TextBox Text="{Binding VolcengineEndpointId}" Height="34" Background="White" Foreground="{StaticResource Slate800}" BorderBrush="{StaticResource Gray200}" Padding="10,6"/>

                        <TextBlock Text="默认模型" Foreground="{StaticResource Slate700}" Margin="0,12,0,0"/>
                        <TextBox Text="{Binding VolcengineDefaultModel}" Height="34" Background="White" Foreground="{StaticResource Slate800}" BorderBrush="{StaticResource Gray200}" Padding="10,6"/>

                        <TextBlock Text="Endpoint" Foreground="{StaticResource Slate700}" Margin="0,12,0,0"/>
                        <TextBox Text="{Binding VolcengineEndpoint}" Height="34" Background="White" Foreground="{StaticResource Slate800}" BorderBrush="{StaticResource Gray200}" Padding="10,6"/>

                        <TextBlock Text="超时（秒）" Foreground="{StaticResource Slate700}" Margin="0,12,0,0"/>
                        <TextBox Text="{Binding VolcengineTimeoutSeconds}" Height="34" Background="White" Foreground="{StaticResource Slate800}" BorderBrush="{StaticResource Gray200}" Padding="10,6"/>
                    </StackPanel>
                </TabItem>
            </TabControl>

            <!-- Validation results (in-page) -->
            <Border Background="White" BorderBrush="{StaticResource Gray200}" BorderThickness="1" CornerRadius="12" Padding="20" Margin="0,20,0,0">
                <StackPanel>
                    <TextBlock Text="验证结果" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource Slate800}" Margin="0,0,0,12"/>

                    <DataGrid ItemsSource="{Binding ValidationResults}" AutoGenerateColumns="False" CanUserAddRows="False"
                              HeadersVisibility="Column" GridLinesVisibility="Horizontal" RowHeight="34"
                              Background="White" Foreground="{StaticResource Slate800}" BorderThickness="0"
                              HorizontalGridLinesBrush="{StaticResource Gray100}" VerticalGridLinesBrush="Transparent"
                              ColumnHeaderStyle="{StaticResource LightDataGridColumnHeaderStyle}"
                              CellStyle="{StaticResource LightDataGridCellStyle}"
                              RowStyle="{StaticResource LightDataGridRowStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="时间" Binding="{Binding Timestamp, StringFormat=HH:mm:ss}" Width="120" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Provider" Binding="{Binding Provider}" Width="180" IsReadOnly="True"/>
                            <DataGridTextColumn Header="结果" Binding="{Binding ResultText}" Width="100" IsReadOnly="True"/>
                            <DataGridTextColumn Header="详情" Binding="{Binding Message}" Width="*" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource Slate500}" FontSize="12" Margin="0,12,0,0" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="↻ 重新加载" Command="{Binding ReloadCommand}" Margin="0,0,12,0" Padding="16,10"/>
                <Button Content="✅ 验证配置" Command="{Binding ValidateCommand}" Margin="0,0,12,0" Padding="16,10"/>
                <Button Content="💾 保存配置" Command="{Binding SaveCommand}" Style="{StaticResource PrimaryButton}" Padding="24,12" FontSize="14"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
