<Window x:Class="Storyboard.Views.Windows.ImagePreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="预览" Height="720" Width="1100"
        WindowStartupLocation="CenterOwner"
        Background="#0F172A"
        AllowsTransparency="False">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="True" Margin="0,0,0,12">
            <TextBlock Text="素材预览" Foreground="#E5E7EB" FontSize="16" FontWeight="SemiBold" DockPanel.Dock="Left"/>
            <Button Content="关闭" Width="90" HorizontalAlignment="Right" DockPanel.Dock="Right"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Click="Close_Click"/>
        </DockPanel>

        <Border Grid.Row="1" CornerRadius="10" Background="#111827" BorderBrush="#1F2937" BorderThickness="1">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <Image x:Name="PreviewImage" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
