<Window x:Class="分镜大师.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="clr-namespace:分镜大师.Converters"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    mc:Ignorable="d"
    Title="Storyboard Editor" Height="900" Width="1800"
    WindowStartupLocation="CenterScreen"
    Background="#F0F2F5">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <converters:InverseBoolConverter x:Key="InverseBoolConverter"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility"/>
        <converters:DoubleConverter x:Key="DoubleConverter"/>
        
        <!-- Tailwind Color Resources -->
        <SolidColorBrush x:Key="Gray50" Color="#F9FAFB"/>
        <SolidColorBrush x:Key="Gray100" Color="#F3F4F6"/>
        <SolidColorBrush x:Key="Gray200" Color="#E5E7EB"/>
        <SolidColorBrush x:Key="Gray300" Color="#D1D5DB"/>
        <SolidColorBrush x:Key="Gray400" Color="#9CA3AF"/>
        <SolidColorBrush x:Key="Gray500" Color="#6B7280"/>
        <SolidColorBrush x:Key="Gray600" Color="#4B5563"/>
        <SolidColorBrush x:Key="Slate400" Color="#94A3B8"/>
        <SolidColorBrush x:Key="Slate600" Color="#475569"/>
        <SolidColorBrush x:Key="Slate700" Color="#334155"/>
        <SolidColorBrush x:Key="Slate800" Color="#1E293B"/>
        <SolidColorBrush x:Key="Blue50" Color="#EFF6FF"/>
        <SolidColorBrush x:Key="Blue500" Color="#3B82F6"/>
        <SolidColorBrush x:Key="Blue600" Color="#2563EB"/>
        <SolidColorBrush x:Key="Blue700" Color="#1D4ED8"/>
        <SolidColorBrush x:Key="Green50" Color="#F0FDF4"/>
        <SolidColorBrush x:Key="Green500" Color="#22C55E"/>
        <SolidColorBrush x:Key="Green600" Color="#16A34A"/>
        <SolidColorBrush x:Key="Green700" Color="#15803D"/>
        <SolidColorBrush x:Key="Yellow50" Color="#FEFCE8"/>
        <SolidColorBrush x:Key="Yellow500" Color="#EAB308"/>
        <SolidColorBrush x:Key="Yellow700" Color="#A16207"/>
        <SolidColorBrush x:Key="Orange50" Color="#FFF7ED"/>
        <SolidColorBrush x:Key="Orange100" Color="#FFEDD5"/>
        <SolidColorBrush x:Key="Orange500" Color="#F97316"/>
        <SolidColorBrush x:Key="Orange600" Color="#EA580C"/>
        <SolidColorBrush x:Key="Red50" Color="#FEF2F2"/>
        <SolidColorBrush x:Key="Purple600" Color="#9333EA"/>
        
        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3E3E42"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#505050"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1084D8"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#006CBC"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Success Button Style -->
        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#16A34A"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#15803D"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Small Action Button Style -->
        <Style x:Key="SmallActionButton" TargetType="Button">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#3E3E42"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2D2D30"/>
                    <Setter Property="BorderBrush" Value="#0078D4"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <!-- Top navigation pill style -->
        <Style x:Key="TopNavRadioButton" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource Slate600}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="NavItemBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="999">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="NavItemBorder" Property="Background" Value="{StaticResource Blue50}"/>
                                <Setter Property="Foreground" Value="{StaticResource Blue600}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="NavItemBorder" Property="Background" Value="{StaticResource Blue50}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Light DataGrid header/cell/row styles -->
        <Style x:Key="LightDataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F9FAFB"/>
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <TextBlock Text="{TemplateBinding Content}"
                                       Foreground="#6B7280"
                                       FontWeight="{TemplateBinding FontWeight}"
                                       FontSize="{TemplateBinding FontSize}"
                                       HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                       VerticalAlignment="Center"
                                       TextOptions.TextFormattingMode="Display"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="LightDataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"
                                              TextElement.Foreground="#334155"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="LightDataGridRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="BorderBrush" Value="#F3F4F6"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridRow">
                        <Border x:Name="DGR_Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="True">
                            <SelectiveScrollingGrid>
                                <SelectiveScrollingGrid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </SelectiveScrollingGrid.ColumnDefinitions>
                                <SelectiveScrollingGrid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </SelectiveScrollingGrid.RowDefinitions>
                                <DataGridCellsPresenter Grid.Column="1"
                                                        ItemsPanel="{TemplateBinding ItemsPanel}"
                                                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                <DataGridDetailsPresenter Grid.Column="1" Grid.Row="1"
                                                          SelectiveScrollingGrid.SelectiveScrollingOrientation="{Binding AreRowDetailsFrozen, ConverterParameter={x:Static SelectiveScrollingOrientation.Vertical}, Converter={x:Static DataGrid.RowDetailsScrollingConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                                          Visibility="{TemplateBinding DetailsVisibility}"/>
                                <DataGridRowHeader Grid.RowSpan="2"
                                                   SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                                                   Visibility="{Binding HeadersVisibility, ConverterParameter={x:Static DataGridHeadersVisibility.Row}, Converter={x:Static DataGrid.HeadersVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"/>
                            </SelectiveScrollingGrid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="DGR_Border" Property="Background" Value="#EFF6FF4D"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="DGR_Border" Property="Background" Value="#DBEAFE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

            <!-- 顶部标题栏 -->
            <Border Grid.Row="0" Padding="24,16" Background="White" BorderBrush="{StaticResource Gray200}" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="📹" FontSize="24" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="Storyboard Editor" FontSize="18" FontWeight="Bold" Foreground="{StaticResource Slate800}"/>
                                <TextBlock Text="v2.6.1" FontSize="12" Foreground="{StaticResource Gray400}" Margin="8,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Text="{Binding Shots.Count, Mode=OneWay, StringFormat=共 {0} 个分镜 · 拖拽镜头号调整顺序}" 
                                       Foreground="{StaticResource Gray500}" FontSize="11" Margin="0,2,0,0"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="24,0,0,0">
                        <RadioButton x:Name="NavStoryboard" Content="分镜管理" IsChecked="True" GroupName="TopNavGroup" Style="{StaticResource TopNavRadioButton}"/>
                        <RadioButton x:Name="NavUsageDoc" Content="使用文档" GroupName="TopNavGroup" Style="{StaticResource TopNavRadioButton}" Margin="12,0,0,0"/>
                        <RadioButton x:Name="NavApiKey" Content="密钥配置" GroupName="TopNavGroup" Style="{StaticResource TopNavRadioButton}" Margin="12,0,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                        <Border Background="{StaticResource Gray50}" BorderBrush="{StaticResource Gray200}" 
                                BorderThickness="1" CornerRadius="8" Padding="12,8" Width="256" Margin="0,0,12,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="🔍" FontSize="14" Foreground="{StaticResource Gray400}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBox Grid.Column="1" Background="Transparent" BorderThickness="0" 
                                         Foreground="{StaticResource Slate800}" VerticalAlignment="Center" Text="搜索关键词..."/>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                        <Rectangle Width="1" Height="16" Fill="{StaticResource Gray300}" Margin="0,0,16,0"/>
                        <Button Content="⚙️" FontSize="18" Background="Transparent" BorderThickness="0" 
                                Width="32" Height="32" Padding="6" Margin="0,0,16,0" Cursor="Hand"/>
                       
                    </StackPanel>
                </Grid>
            </Border>

        <!-- 内容区域 -->
        <Grid Grid.Row="1">
            <!-- 分镜管理页面 -->
            <Grid x:Name="StoryboardPage" Visibility="{Binding IsChecked, ElementName=NavStoryboard, Converter={StaticResource BoolToVisibility}}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 工具栏 -->
                    <Border Grid.Row="0" Background="White" CornerRadius="12" Padding="16" Margin="20,20,20,20"
                            BorderBrush="{StaticResource Gray100}" BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <Button Command="{Binding UploadVideoCommand}" Margin="0,0,12,0" Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="{StaticResource Blue600}"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Padding" Value="16,8"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                        <StackPanel Orientation="Horizontal">
                                                            <TextBlock Text="➕" FontSize="16" Margin="0,0,8,0"/>
                                                            <TextBlock Text="添加分镜"/>
                                                        </StackPanel>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{StaticResource Blue700}"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>

                            <Button Margin="0,0,12,0" Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="White"/>
                                        <Setter Property="Foreground" Value="{StaticResource Slate700}"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource Gray200}"/>
                                        <Setter Property="Padding" Value="16,8"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" 
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                            CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                        <StackPanel Orientation="Horizontal">
                                                            <TextBlock Text="✨" FontSize="16" Foreground="{StaticResource Orange500}" Margin="0,0,8,0"/>
                                                            <TextBlock Text="批量生成图片"/>
                                                        </StackPanel>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{StaticResource Gray50}"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>

                            <Button Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="White"/>
                                        <Setter Property="Foreground" Value="{StaticResource Slate700}"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource Gray200}"/>
                                        <Setter Property="Padding" Value="16,8"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" 
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                            CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                        <StackPanel Orientation="Horizontal">
                                                            <TextBlock Text="🎬" FontSize="16" Foreground="{StaticResource Purple600}" Margin="0,0,8,0"/>
                                                            <TextBlock Text="一键合成视频"/>
                                                        </StackPanel>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{StaticResource Gray50}"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>
                    </Border>

                    <!-- 主数据表格 -->
                    <Border Grid.Row="1" Background="White" BorderBrush="{StaticResource Gray100}" BorderThickness="1" Margin="20,0,20,16" CornerRadius="12">
                        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                            <DataGrid x:Name="ShotsDataGrid"
                                      Style="{x:Null}"
                                      ItemsSource="{Binding Shots}"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      CanUserReorderColumns="False"
                                      CanUserResizeRows="False"
                                      SelectionMode="Single"
                                      GridLinesVisibility="Horizontal"
                                      HeadersVisibility="Column"
                                      RowHeight="60"
                                      FontSize="14"
                                      AllowDrop="True"
                                      PreviewMouseLeftButtonDown="DataGrid_PreviewMouseLeftButtonDown"
                                      Drop="DataGrid_Drop"
                                      ColumnHeaderHeight="44"
                                      RowHeaderWidth="0"
                                      SnapsToDevicePixels="True"
                                      FrozenColumnCount="1"
                                      Background="White"
                                      Foreground="{StaticResource Slate800}"
                                      BorderThickness="0"
                                      HorizontalGridLinesBrush="{StaticResource Gray100}"
                                      VerticalGridLinesBrush="Transparent"
                                      ColumnHeaderStyle="{StaticResource LightDataGridColumnHeaderStyle}"
                                      CellStyle="{StaticResource LightDataGridCellStyle}"
                                      RowStyle="{StaticResource LightDataGridRowStyle}">

                                <DataGrid.Columns>
                                    <!-- #列（固定列） -->
                                    <DataGridTextColumn Header="#" Binding="{Binding ShotNumber}" Width="60" IsReadOnly="True">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                                <Setter Property="FontSize" Value="14"/>
                                                <Setter Property="FontFamily" Value="Consolas,Courier New"/>
                                                <Setter Property="Foreground" Value="#9CA3AF"/>
                                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>

                                    <!-- 画面提示词(PROMPT) -->
                                    <DataGridTextColumn Header="画面提示词 (PROMPT)" Binding="{Binding FirstFramePrompt}" Width="*">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="TextWrapping" Value="Wrap"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="Foreground" Value="#334155"/>
                                                <Setter Property="FontSize" Value="13"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>

                                    <!-- 图片状态 -->
                                    <DataGridTemplateColumn Header="图片状态" Width="192">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Ellipse Width="8" Height="8" Margin="0,0,8,0">
                                                        <Ellipse.Style>
                                                            <Style TargetType="Ellipse">
                                                                <Setter Property="Fill" Value="{StaticResource Yellow500}"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding FirstFrameImagePath, Converter={StaticResource InverseBoolToVisibility}}" Value="Visible">
                                                                        <Setter Property="Fill" Value="{StaticResource Green500}"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding IsFirstFrameGenerating}" Value="True">
                                                                        <Setter Property="Fill" Value="{StaticResource Blue500}"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Ellipse.Style>
                                                    </Ellipse>
                                                    <Border CornerRadius="12" Padding="8,4">
                                                        <Border.Style>
                                                            <Style TargetType="Border">
                                                                <Setter Property="Background" Value="{StaticResource Yellow50}"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding FirstFrameImagePath, Converter={StaticResource InverseBoolToVisibility}}" Value="Visible">
                                                                        <Setter Property="Background" Value="{StaticResource Green50}"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding IsFirstFrameGenerating}" Value="True">
                                                                        <Setter Property="Background" Value="{StaticResource Blue50}"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Border.Style>
                                                        <TextBlock FontSize="12" FontWeight="Medium">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Foreground" Value="{StaticResource Yellow700}"/>
                                                                    <Setter Property="Text" Value="排队中..."/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding FirstFrameImagePath, Converter={StaticResource InverseBoolToVisibility}}" Value="Visible">
                                                                            <Setter Property="Foreground" Value="{StaticResource Green700}"/>
                                                                            <Setter Property="Text" Value="生成成功"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding IsFirstFrameGenerating}" Value="True">
                                                                            <Setter Property="Foreground" Value="{StaticResource Blue700}"/>
                                                                            <Setter Property="Text" Value="生成中..."/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                    </Border>
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <!-- 视频状态 -->
                                    <DataGridTemplateColumn Header="视频状态" Width="192">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Ellipse Width="8" Height="8" Margin="0,0,8,0">
                                                        <Ellipse.Style>
                                                            <Style TargetType="Ellipse">
                                                                <Setter Property="Fill" Value="{StaticResource Slate600}"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding GeneratedVideoPath, Converter={StaticResource InverseBoolToVisibility}}" Value="Visible">
                                                                        <Setter Property="Fill" Value="{StaticResource Green500}"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding IsVideoGenerating}" Value="True">
                                                                        <Setter Property="Fill" Value="{StaticResource Blue500}"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Ellipse.Style>
                                                    </Ellipse>
                                                    <Border CornerRadius="12" Padding="8,4">
                                                        <Border.Style>
                                                            <Style TargetType="Border">
                                                                <Setter Property="Background" Value="{StaticResource Gray100}"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding GeneratedVideoPath, Converter={StaticResource InverseBoolToVisibility}}" Value="Visible">
                                                                        <Setter Property="Background" Value="{StaticResource Green50}"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding IsVideoGenerating}" Value="True">
                                                                        <Setter Property="Background" Value="{StaticResource Blue50}"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Border.Style>
                                                        <TextBlock FontSize="12" FontWeight="Medium">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Foreground" Value="{StaticResource Slate600}"/>
                                                                    <Setter Property="Text" Value="等待渲染"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding GeneratedVideoPath, Converter={StaticResource InverseBoolToVisibility}}" Value="Visible">
                                                                            <Setter Property="Foreground" Value="{StaticResource Green700}"/>
                                                                            <Setter Property="Text" Value="渲染完成"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding IsVideoGenerating}" Value="True">
                                                                            <Setter Property="Foreground" Value="{StaticResource Blue700}"/>
                                                                            <Setter Property="Text" Value="渲染中..."/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                    </Border>
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <!-- 预览与操作 -->
                                    <DataGridTemplateColumn Header="预览与操作" Width="224">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                    <!-- 查看按钮 -->
                                                    <Button Width="36" Height="36" Margin="0,0,8,0" Cursor="Hand"
                                                            Command="{Binding DataContext.ViewFirstFrameImageCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                            CommandParameter="{Binding}">
                                                        <Button.Style>
                                                            <Style TargetType="Button">
                                                                <Setter Property="Background" Value="White"/>
                                                                <Setter Property="BorderBrush" Value="{StaticResource Gray100}"/>
                                                                <Setter Property="BorderThickness" Value="1"/>
                                                                <Setter Property="Template">
                                                                    <Setter.Value>
                                                                        <ControlTemplate TargetType="Button">
                                                                            <Border Background="{TemplateBinding Background}" 
                                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                                                    CornerRadius="4">
                                                                                <TextBlock Text="🔍" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                            </Border>
                                                                        </ControlTemplate>
                                                                    </Setter.Value>
                                                                </Setter>
                                                                <Style.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter Property="Background" Value="{StaticResource Gray50}"/>
                                                                    </Trigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Button.Style>
                                                    </Button>

                                                    <!-- 播放按钮 -->
                                                    <Button Width="36" Height="36" Margin="0,0,8,0" Cursor="Hand"
                                                            Command="{Binding DataContext.PlayVideoCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                            CommandParameter="{Binding}">
                                                        <Button.Style>
                                                            <Style TargetType="Button">
                                                                <Setter Property="Background" Value="White"/>
                                                                <Setter Property="BorderBrush" Value="{StaticResource Gray100}"/>
                                                                <Setter Property="BorderThickness" Value="1"/>
                                                                <Setter Property="Template">
                                                                    <Setter.Value>
                                                                        <ControlTemplate TargetType="Button">
                                                                            <Border Background="{TemplateBinding Background}" 
                                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                                                    CornerRadius="4">
                                                                                <TextBlock Text="▶" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                            </Border>
                                                                        </ControlTemplate>
                                                                    </Setter.Value>
                                                                </Setter>
                                                                <Style.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter Property="Background" Value="{StaticResource Gray50}"/>
                                                                    </Trigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Button.Style>
                                                    </Button>

                                                    <!-- 文件按钮 -->
                                                    <Button Width="36" Height="36" Margin="0,0,8,0" Cursor="Hand"
                                                            Command="{Binding DataContext.OpenFirstFrameFolderCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                            CommandParameter="{Binding}">
                                                        <Button.Style>
                                                            <Style TargetType="Button">
                                                                <Setter Property="Background" Value="White"/>
                                                                <Setter Property="BorderBrush" Value="{StaticResource Gray100}"/>
                                                                <Setter Property="BorderThickness" Value="1"/>
                                                                <Setter Property="Template">
                                                                    <Setter.Value>
                                                                        <ControlTemplate TargetType="Button">
                                                                            <Border Background="{TemplateBinding Background}" 
                                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                                                    CornerRadius="4">
                                                                                <TextBlock Text="📁" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                            </Border>
                                                                        </ControlTemplate>
                                                                    </Setter.Value>
                                                                </Setter>
                                                                <Style.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter Property="Background" Value="{StaticResource Gray50}"/>
                                                                    </Trigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Button.Style>
                                                    </Button>

                                                    <!-- 删除按钮 -->
                                                    <Button Width="36" Height="36" Cursor="Hand">
                                                        <Button.Style>
                                                            <Style TargetType="Button">
                                                                <Setter Property="Background" Value="White"/>
                                                                <Setter Property="BorderBrush" Value="{StaticResource Gray100}"/>
                                                                <Setter Property="BorderThickness" Value="1"/>
                                                                <Setter Property="Template">
                                                                    <Setter.Value>
                                                                        <ControlTemplate TargetType="Button">
                                                                            <Border Background="{TemplateBinding Background}" 
                                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                                                    CornerRadius="4">
                                                                                <TextBlock Text="🗑️" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                            </Border>
                                                                        </ControlTemplate>
                                                                    </Setter.Value>
                                                                </Setter>
                                                                <Style.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter Property="Background" Value="{StaticResource Red50}"/>
                                                                        <Setter Property="BorderBrush" Value="{StaticResource Red500}"/>
                                                                    </Trigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Button.Style>
                                                    </Button>
                                                </StackPanel>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </ScrollViewer>
                    </Border>

                    <!-- 底部统计栏 -->
                    <Border Grid.Row="2" Background="White" BorderBrush="{StaticResource Gray100}" BorderThickness="0,1,0,0" Padding="24,12" Margin="20,0,20,20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Foreground="{StaticResource Slate600}" FontSize="12" Margin="0,0,24,0">
                                    <Run Text="总分镜 "/>
                                    <Run Text="{Binding Shots.Count, Mode=OneWay}" FontWeight="Bold" Foreground="{StaticResource Slate800}"/>
                                </TextBlock>
                                <TextBlock Foreground="{StaticResource Slate600}" FontSize="12" Margin="0,0,24,0">
                                    <Run Text="已生成 "/>
                                    <Run Text="{Binding CompletedShotsCount}" FontWeight="Bold" Foreground="#16A34A"/>
                                </TextBlock>
                                <TextBlock Foreground="{StaticResource Slate600}" FontSize="12">
                                    <Run Text="渲染中 "/>
                                    <Run Text="{Binding ShotsRenderingCount}" FontWeight="Bold" Foreground="#F97316"/>
                                </TextBlock>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Foreground="{StaticResource Slate600}" FontSize="12">
                                    <Run Text="🖼 已生成图像 "/>
                                    <Run Text="{Binding GeneratedImagesCount}" FontWeight="Bold" Foreground="#16A34A"/>
                                </TextBlock>
                                <TextBlock Foreground="{StaticResource Slate600}" FontSize="12" Margin="24,0,0,0">
                                    <Run Text="🎬 已生成视频 "/>
                                    <Run Text="{Binding GeneratedVideosCount}" FontWeight="Bold" Foreground="#0078D4"/>
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                    </Border>
    </Grid>
</Grid>

            <!-- 使用文档页面 -->
            <Grid x:Name="UsageDocPage" Visibility="{Binding IsChecked, ElementName=NavUsageDoc, Converter={StaticResource BoolToVisibility}}">
                <ScrollViewer Padding="40" Background="#0F0F0F">
                    <StackPanel MaxWidth="900">
                        <TextBlock Text="📖 使用文档" FontSize="28" FontWeight="Bold" Foreground="#E0E0E0" Margin="0,0,0,24"/>
                        
                        <Border Background="#1E1E1E" BorderBrush="#3E3E42" BorderThickness="1" Padding="24" Margin="0,0,0,20">
                            <StackPanel>
                                <TextBlock Text="1. 上传视频" FontSize="18" FontWeight="SemiBold" Foreground="#0078D4" Margin="0,0,0,12"/>
                                <TextBlock TextWrapping="Wrap" Foreground="#CCCCCC" Margin="0,0,0,8">
                                    点击右上角"📁 上传视频"按钮，选择需要分析的视频文件。系统支持 MP4、AVI、MOV、MKV 等常见格式。
                                </TextBlock>
                            </StackPanel>
                        </Border>

                        <Border Background="#1E1E1E" BorderBrush="#3E3E42" BorderThickness="1" Padding="24" Margin="0,0,0,20">
                            <StackPanel>
                                <TextBlock Text="2. AI 分镜分析" FontSize="18" FontWeight="SemiBold" Foreground="#0078D4" Margin="0,0,0,12"/>
                                <TextBlock TextWrapping="Wrap" Foreground="#CCCCCC" Margin="0,0,0,8">
                                    上传后，AI 会自动分析视频内容，识别镜头切换点，并为每个镜头生成：
                                </TextBlock>
                                <TextBlock TextWrapping="Wrap" Foreground="#CCCCCC" Margin="16,0,0,4">• 镜头时长</TextBlock>
                                <TextBlock TextWrapping="Wrap" Foreground="#CCCCCC" Margin="16,0,0,4">• 首帧和尾帧提示词</TextBlock>
                                <TextBlock TextWrapping="Wrap" Foreground="#CCCCCC" Margin="16,0,0,4">• 镜头类型、核心画面、动作指令</TextBlock>
                                <TextBlock TextWrapping="Wrap" Foreground="#CCCCCC" Margin="16,0,0,4">• 场景设定</TextBlock>
                            </StackPanel>
                        </Border>

                        <Border Background="#1E1E1E" BorderBrush="#3E3E42" BorderThickness="1" Padding="24" Margin="0,0,0,20">
                            <StackPanel>
                                <TextBlock Text="3. 编辑分镜参数" FontSize="18" FontWeight="SemiBold" Foreground="#0078D4" Margin="0,0,0,12"/>
                                <TextBlock TextWrapping="Wrap" Foreground="#CCCCCC" Margin="0,0,0,8">
                                    点击任意单元格即可编辑内容。您可以：
                                </TextBlock>
                                <TextBlock TextWrapping="Wrap" Foreground="#CCCCCC" Margin="16,0,0,4">• 修改提示词以获得更精确的图像</TextBlock>
                                <TextBlock TextWrapping="Wrap" Foreground="#CCCCCC" Margin="16,0,0,4">• 调整镜头时长</TextBlock>
                                <TextBlock TextWrapping="Wrap" Foreground="#CCCCCC" Margin="16,0,0,4">• 选择不同的生成模型</TextBlock>
                                <TextBlock TextWrapping="Wrap" Foreground="#CCCCCC" Margin="16,0,0,4">• 拖动镜头号调整顺序</TextBlock>
                            </StackPanel>
                        </Border>

                        <Border Background="#1E1E1E" BorderBrush="#3E3E42" BorderThickness="1" Padding="24" Margin="0,0,0,20">
                            <StackPanel>
                                <TextBlock Text="4. 生成图像" FontSize="18" FontWeight="SemiBold" Foreground="#0078D4" Margin="0,0,0,12"/>
                                <TextBlock TextWrapping="Wrap" Foreground="#CCCCCC" Margin="0,0,0,8">
                                    点击"🎨 生成首帧"和"🎨 生成尾帧"按钮，使用 AI 生成对应的图像。生成后可以：
                                </TextBlock>
                                <TextBlock TextWrapping="Wrap" Foreground="#CCCCCC" Margin="16,0,0,4">• 🔍 查看大图 - 在新窗口中预览完整图像</TextBlock>
                                <TextBlock TextWrapping="Wrap" Foreground="#CCCCCC" Margin="16,0,0,4">• 📂 文件位置 - 在文件管理器中打开图像所在文件夹</TextBlock>
                            </StackPanel>
                        </Border>

                        <Border Background="#1E1E1E" BorderBrush="#3E3E42" BorderThickness="1" Padding="24">
                            <StackPanel>
                                <TextBlock Text="5. 生成视频" FontSize="18" FontWeight="SemiBold" Foreground="#0078D4" Margin="0,0,0,12"/>
                                <TextBlock TextWrapping="Wrap" Foreground="#CCCCCC" Margin="0,0,0,8">
                                    当首帧和尾帧都生成完成后，点击"🎬 生成视频"，系统会根据两帧图像和提示词生成过渡动画。生成后可以：
                                </TextBlock>
                                <TextBlock TextWrapping="Wrap" Foreground="#CCCCCC" Margin="16,0,0,4">• ▶ 观看视频 - 使用默认播放器播放视频</TextBlock>
                                <TextBlock TextWrapping="Wrap" Foreground="#CCCCCC" Margin="16,0,0,4">• 📂 文件位置 - 在文件管理器中打开视频所在文件夹</TextBlock>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Grid>

            <!-- 密钥配置页面 -->
            <Grid x:Name="ApiKeyPage" Visibility="{Binding IsChecked, ElementName=NavApiKey, Converter={StaticResource BoolToVisibility}}">
                <ScrollViewer Padding="40" Background="#0F0F0F">
                    <StackPanel MaxWidth="700">
                        <TextBlock Text="🔑 API 密钥配置" FontSize="28" FontWeight="Bold" Foreground="#E0E0E0" Margin="0,0,0,24"/>
                        
                        <Border Background="#1E1E1E" BorderBrush="#3E3E42" BorderThickness="1" Padding="24" Margin="0,0,0,20">
                            <StackPanel>
                                <TextBlock Text="Runway Gen3 API Key" FontSize="14" FontWeight="SemiBold" Foreground="#E0E0E0" Margin="0,0,0,8"/>
                                <TextBox Height="36" Background="#2D2D30" Foreground="#E0E0E0" BorderBrush="#3E3E42" Padding="12,8" FontSize="13"/>
                                <TextBlock Text="用于生成视频的 Runway Gen3 API 密钥" Foreground="#808080" FontSize="11" Margin="0,6,0,0"/>
                            </StackPanel>
                        </Border>

                        <Border Background="#1E1E1E" BorderBrush="#3E3E42" BorderThickness="1" Padding="24" Margin="0,0,0,20">
                            <StackPanel>
                                <TextBlock Text="Stable Diffusion API Key" FontSize="14" FontWeight="SemiBold" Foreground="#E0E0E0" Margin="0,0,0,8"/>
                                <TextBox Height="36" Background="#2D2D30" Foreground="#E0E0E0" BorderBrush="#3E3E42" Padding="12,8" FontSize="13"/>
                                <TextBlock Text="用于生成图像的 Stable Diffusion API 密钥" Foreground="#808080" FontSize="11" Margin="0,6,0,0"/>
                            </StackPanel>
                        </Border>

                        <Border Background="#1E1E1E" BorderBrush="#3E3E42" BorderThickness="1" Padding="24" Margin="0,0,0,20">
                            <StackPanel>
                                <TextBlock Text="Pika API Key" FontSize="14" FontWeight="SemiBold" Foreground="#E0E0E0" Margin="0,0,0,8"/>
                                <TextBox Height="36" Background="#2D2D30" Foreground="#E0E0E0" BorderBrush="#3E3E42" Padding="12,8" FontSize="13"/>
                                <TextBlock Text="用于生成视频的 Pika API 密钥" Foreground="#808080" FontSize="11" Margin="0,6,0,0"/>
                            </StackPanel>
                        </Border>

                        <Button Content="💾 保存配置" 
                                Style="{StaticResource PrimaryButton}"
                                Padding="24,12"
                                FontSize="14"
                                HorizontalAlignment="Right"/>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>

        <!-- 加载遮罩 -->
        <Grid Grid.RowSpan="2" Background="#CC000000" Visibility="{Binding IsAnalyzing, Converter={StaticResource BoolToVisibility}}">
            <Border Background="#1E1E1E" BorderBrush="#0078D4" BorderThickness="2" 
                    CornerRadius="8" Padding="40,32"
                    HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="🔄" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                    <TextBlock Text="AI 正在分析视频..." FontSize="20" Foreground="#E0E0E0" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                    <TextBlock Text="请稍候，这可能需要几分钟时间..." FontSize="12" Foreground="#808080" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
